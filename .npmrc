# Use a flat node_modules so the dependency tree survives Amplify's build-cache
# tar/untar. pnpm's default symlinked layout breaks on cache restore, causing
# "Cannot find module 'styled-jsx/package.json'" on the second+ build.
node-linker=hoisted
